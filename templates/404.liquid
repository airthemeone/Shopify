<div id="columns-{{ section.id }}" class="container mx-auto px-3 sm:px-4 xl:px-0 py-8">
    <div class="w-full">
        <h1 class="text-4xl font-extrabold leading-tight text-font-color-darkest mb-4 text-center">
            {{ 'general.404.title' | t }}
        </h1>
        <div class="mx-auto prose text-center mt-8">
            <p>{{ 'general.404.subtext' | t }}</p>
        </div>
    </div>
    <div class="text-center mt-8">
        <a 
            href="{{ routes.root_url }}" 
            class="inline-flex items-center px-5 py-3  border border-gray-300 border-transparent font-medium rounded-md text-button-primary-text bg-button-primary hover:bg-button-primary-hover focus:outline-none focus:bg-button-primary-hover focus:shadow-outline-primary active:bg-button-primary-hover transition ease-in-out duration-150"
        >
            {{ 'general.404.link' | t }}
        </a>
    </div>
</div>
  